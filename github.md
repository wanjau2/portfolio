# GitHub source

repo: wanjau2/portfolio
branch: finale

## Last sync

date: 2026-08-20T12:10:19Z

### Updated in this project

- Read the repo file listing only (index.html, main.js, styles.css) — no repo content imported.
- Portfolio page in this project was authored from Eugene's three CV documents, not from the repo.
- A repo-ready static export lives in `export/` (index.html, styles.css, assets/, README.md) for manual commit.

## Screen map

| Project screen | Built from |
| --- | --- |
| `Eugene Wanjau - Portfolio.dc.html` | `uploads/*.docx` (three CVs) — not repo files |
| `export/index.html`, `export/styles.css` | static export of the above; intended to replace repo `index.html` / `styles.css` |

## Notes

GitHub access here is read-only — repo files can be read and copied in, but nothing
can be committed or pushed from this project. Publishing means downloading `export/`
and committing it locally.
